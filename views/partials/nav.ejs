<nav class="navbar navbar-expand-lg navbar-dark dark-div">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Radiator Springs University</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <% if (user) { %>
            <li class="nav-item"><span class="nav-link active">Hello, <%= user.displayName %></span></li>

            
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/">Home</a>
          </li>
            <% if (user.userGroup === "Instructor") { %>
            <li class="nav-item">
              <a class="nav-link" href="/instructor">Modify Courses</a>
            </li>
            <% } else if (user.userGroup === "Student") { %>
            <li class="nav-item">
              <a class="nav-link" href="/student">Add Courses</a>
            </li>
            <% } %>
          <li class="nav-item">
            <a class="nav-link" href="/logout">Log Out</a>
          </li>
          <% } else { %>
            
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/login">Log In</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/signup">Sign Up</a>
          </li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>